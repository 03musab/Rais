services:
  # Backend + Frontend (Express serves static HTML)
  - type: web
    name: rais-embroidery
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production